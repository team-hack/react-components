import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { Divider } from './Divider'

<Meta title='MDX/Divider' component={Divider} />

export const Template = (args) => <Divider {...args} />

# Divider

This is the primary Divider. Use it for actions that you want to draw attention to.

<Canvas>
  <Story
    name='Primary'
    args={{
      primary: true,
      label: 'Divider',
      onClick: () => console.log('🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥')
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

This is the secondary Divider. You'll use this the rest of the time.

<Canvas>
  <Story
    name='Secondary'
    args={{
      label: 'Divider',
      onClick: () => console.log('🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥')
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

And now some different Divider sizes:

<Canvas>
  <Story
    name='Small Divider'
    args={{
      label: 'Small',
      size: 'small',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Medium Divider'
    args={{
      label: 'Medium',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Large Divider'
    args={{
      label: 'Large',
      size: 'large'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
